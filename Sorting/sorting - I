1. Selection Sort Algorithm:
    swap minimum element of array with the first element
    i -> 0 - <n-1
    j -> i+1 - <n

    for (let i = 0; i < n - 1; i++) {
        let min = i;
        for (let j = i + 1; j < n; j++) {
            if (a[j] < a[min]) {
                min = j;
            }
        }
        if (min !== i) {
            let temp = a[i];
            a[i] = a[min];
            a[min] = temp;
        }
    }
    return a.join(" ");
  
  
